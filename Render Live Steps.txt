step 1 - open project in vs code and create virtual env in terminal if it is not.

cmd : dir 

check if there is a env created or not. if not then run this command 

cmd : python -m venv venv

cmd :	 .\venv\Scripts\Activate.ps1

cmd : dir (checked created env is there or not)

cmd : python manage.py check

cmd : python -m pip install django

then check 

cmd : python -m django --version

then run 

cmd : python manage.py check 

cmd : python -m pip install requests

cmd : python manage.py check 

cmd : python -m pip freeze > requirements.txt

cmd : python -m pip install psycopg2-binary

then install 

cmd : python -m pip install dj-database-url

after both installation finish, update requirements.txt

cmd : python -m pip freeze > requirements.txt

check Django again 

cmd : python manage.py check 

test the website 

cmd : python manage.py runserver 

cmd : python -m pip install psycopg2-binary

cmd : Get-Content requirements.txt

check the output of this 

check app name is inside installed apps 

in allowed hosts change it to ['*']

Check your STATIC_URL 

Find:

STATIC_URL = 'static/'

That's okay for now.

We will configure production static files later.

Check your WSGI file

Open:

WeatherMapAPI/
└── WeatherMapAPI/
    └── wsgi.py

It should look approximately like:

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'WeatherMapAPI.settings')

application = get_wsgi_application()

Don't change anything if it already looks like this.

Step 5 — Check your local project again

After checking those files, run:

python manage.py check - 0 silence 

Then:

python manage.py migrate

Step 6 — Check whether your SQLite database has data

You have:

db.sqlite3

Before we switch to PostgreSQL, let's find out whether there is data we need to preserve.

Run:

python manage.py shell

Then paste this:

from django.apps import apps

for model in apps.get_models():
    try:
        print(model._meta.label, model.objects.count())
    except Exception:
        pass

You'll get output such as:

auth.Permission 28
auth.Group 0
weather.SomeModel 15

Then exit:

exit()

Do these now

Run these in order:

1.
python manage.py check
2.
python manage.py migrate
3.
python manage.py shell

Then:

from django.apps import apps

for model in apps.get_models():
    try:
        print(model._meta.label, model.objects.count())
    except Exception:
        pass

Then:

exit()

Send me the output from Step 2 (python manage.py migrate) and Step 3 (the model counts).

After that, I'll take you through creating the PostgreSQL database on Render and connecting this exact project to it.

Step 5 — Keep SQLite for now

Do NOT replace your DATABASES section yet.

Keep:

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

Why?

Because we haven't created the PostgreSQL database on Render yet.

Once we create it, we'll change this to use:

DATABASE_URL

Render provides an internal PostgreSQL connection URL for services in the same region, which is the preferred way to connect your web service to the database.

Step 6 — Install WhiteNoise

Your next deployment requirement is static-file serving.

Render's Django guide recommends WhiteNoise for serving Django static files in production.

Run:

python -m pip install whitenoise

Then update requirements:

python -m pip freeze > requirements.txt

Check:

Get-Content requirements.txt

You should now have:

Django==6.1
dj-database-url==3.1.2
psycopg2-binary==2.9.12
requests==2.34.2
whitenoise==...

Your exact WhiteNoise version may be different.

Step 7 — Configure WhiteNoise

In settings.py, find:

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',

Immediately after SecurityMiddleware, add:

'whitenoise.middleware.WhiteNoiseMiddleware',

So it becomes:

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    ...
]

The position is important: WhiteNoise should come immediately after Django's SecurityMiddleware.

Step 8 — Configure static files

Find:

STATIC_URL = 'static/'

Change/add the production configuration:

STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

So:

STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

Don't delete your existing:

static/

folder.

You'll potentially have:

WeatherMapAPI/
│
├── static/
├── staticfiles/       ← generated later
├── weather/
├── WeatherMapAPI/
├── db.sqlite3
├── manage.py
├── requirements.txt
└── venv/

You do not need to manually create staticfiles/. Django's collectstatic command will create it.

Step 9 — Test everything locally

Run:

python manage.py check

Expected:

System check identified no issues (0 silenced).

Then:

python manage.py collectstatic --noinput

You should see Django collecting static files.

Then:

python manage.py runserver

Open:

http://127.0.0.1:8000/

Make sure your WeatherMapAPI still works.

🚨 Don't create the Render database yet

We're very close, but first I want to make sure your local project survives the production/static-file changes.

Do these now:

1. Install WhiteNoise

python -m pip install whitenoise

2. Update requirements

python -m pip freeze > requirements.txt

3. Make the settings.py changes above.

4. Run:

python manage.py check

5. Run:

python manage.py collectstatic --noinput

6. Run:

python manage.py runserver

Then tell me whether the website opens correctly.

After that, I'll take you through Step 11: creating the Render PostgreSQL database, including exactly what to click in Render. Render currently creates Postgres from Dashboard → New → Postgres.



